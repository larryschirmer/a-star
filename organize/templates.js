let Obj = () => {
	//Error Messages
	//if () throwError('');
	//if () throwError('');

	//Properties
	let properties = {};

	const obj = {};
	Object.setPrototypeOf(obj, Map.prototype);

	return obj;
};
